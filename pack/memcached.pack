{
"name": "memcached",

"description": "Standard memcached checks."

"path":"databases/",

"macros": {
    "_MEMCACHED_ADDRESS": {
        "type":"string",
        "description":"The address of the memcached server. Defaults to HOSTADDRESS."
    }
}
}
